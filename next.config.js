/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true
  }
  // Uncomment and set basePath/assetPrefix if deploying to repo subpath
  // basePath: '/burundi-crochet-fashions',
  // assetPrefix: '/burundi-crochet-fashions/',
};

module.exports = nextConfig;
